spring:
  application:
    name: movie-registry-service
  main:
    banner-mode: "off"

server.port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF